export interface User {
  email: string;
  friender: string;
  friend:string; 
  pairId: string;
}

export interface Chat {
  message: string;
  pair: string;
  sender: string;
  time: number;
}
export interface Test{

}